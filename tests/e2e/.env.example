# =============================================================================
# E2E Test Secrets — REQUIRED (tests hard-fail if missing)
# =============================================================================
#
# Option 1 (preferred): Fetch from GitHub automatically
#   eval $(../../scripts/secrets/read-github-secrets.sh --e2e --env)
#
# Option 2: Copy this file to .env and populate manually
#   cp .env.example .env
#   # Edit .env with values from GitHub Secrets UI
#
# =============================================================================

# Employee test user (test-user.journey)
TEST_USER_PASSWORD=
TEST_USER_TOTP_SECRET=

# Customer portal test users
CUSTOMER_USER_PASSWORD=

# Token exchange for API tests
MCP_INTEGRATION_RUNNER_SECRET=

# Optional overrides (defaults shown)
# TEST_ENV=dev
# TEST_USERNAME=test-user.journey
