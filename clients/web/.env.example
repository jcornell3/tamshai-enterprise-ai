# Tamshai Web Applications - Environment Variables
# Copy this file to .env.local in each app directory and update values as needed

# Keycloak Configuration
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=tamshai-corp
VITE_KEYCLOAK_CLIENT_ID=mcp-gateway

# API Endpoints
VITE_API_GATEWAY_URL=http://localhost:8100
VITE_MCP_GATEWAY_URL=http://localhost:3100

# Application Configuration
VITE_APP_NAME=Tamshai Enterprise AI
VITE_APP_VERSION=1.4.0

# Stage Environment Detection (optional)
# Set this for stage builds to detect VPS IP access without hardcoding
# Get the VPS IP from: cd infrastructure/terraform/vps && terraform output vps_ip
# VITE_STAGE_HOST=
