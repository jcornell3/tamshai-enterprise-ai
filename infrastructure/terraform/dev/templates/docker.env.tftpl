# =============================================================================
# Tamshai Dev Environment - Docker Compose Environment Variables
# =============================================================================
#
# ⚠️ AUTO-GENERATED BY TERRAFORM - DO NOT EDIT MANUALLY ⚠️
#
# This file is generated by Terraform from infrastructure/terraform/dev/main.tf
# To update values, modify dev.tfvars and run: terraform apply -var-file=dev.tfvars
#
# Generated: ${timestamp()}
# Environment: ${environment}
# =============================================================================

# =============================================================================
# DATABASE CREDENTIALS
# =============================================================================

# PostgreSQL
POSTGRES_PASSWORD=${postgres_password}
TAMSHAI_DB_PASSWORD=${tamshai_db_password}

# Keycloak Database
KEYCLOAK_DB_PASSWORD=${keycloak_db_password}

# MongoDB
MONGODB_PASSWORD=${mongodb_password}

# =============================================================================
# KEYCLOAK CONFIGURATION
# =============================================================================

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=${keycloak_admin_password}

# Realm is loaded via Docker's --import-realm flag from:
#   keycloak/realm-export-dev.json (dev/stage)
#   keycloak/realm-export.json (production)
KEYCLOAK_CUSTOMER_REALM=tamshai-customers

# =============================================================================
# OBJECT STORAGE (MinIO)
# =============================================================================

MINIO_ROOT_USER=${minio_root_user}
MINIO_ROOT_PASSWORD=${minio_root_password}

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

REDIS_PASSWORD=${redis_password}

# =============================================================================
# VAULT CONFIGURATION
# =============================================================================

VAULT_DEV_ROOT_TOKEN=${vault_dev_root_token}

# =============================================================================
# MCP GATEWAY CONFIGURATION
# =============================================================================

CLAUDE_API_KEY=${claude_api_key}
MCP_INTERNAL_SECRET=${mcp_internal_secret}
MCP_GATEWAY_CLIENT_SECRET=${mcp_gateway_client_secret}
E2E_ADMIN_API_KEY=${e2e_admin_api_key}
ELASTIC_PASSWORD=${elastic_password}
MCP_UI_CLIENT_SECRET=${mcp_ui_client_secret}
MCP_HR_SERVICE_CLIENT_SECRET=${mcp_hr_service_client_secret}
MCP_INTEGRATION_RUNNER_SECRET=${mcp_integration_runner_secret}

# =============================================================================
# MCP JOURNEY CONFIGURATION (Project History Agent)
# =============================================================================

GEMINI_API_KEY=${gemini_api_key}

# =============================================================================
# ENVIRONMENT
# =============================================================================

ENVIRONMENT=${environment}
NODE_ENV=${environment}

# =============================================================================
# USER CREDENTIALS (from GitHub Secrets)
# =============================================================================

# Password for corporate users (eve.thompson, etc.) - identity-sync
# GitHub secret: DEV_USER_PASSWORD
DEV_USER_PASSWORD=${dev_user_password}

# Password for test-user.journey E2E account - keycloak-sync
# GitHub secret: TEST_USER_PASSWORD
TEST_USER_PASSWORD=${test_user_password}

# Password for customer test users - GitHub secret: CUSTOMER_USER_PASSWORD
CUSTOMER_USER_PASSWORD=${customer_user_password}

# =============================================================================
# PORT CONFIGURATION (from GitHub Variables)
# =============================================================================

# Infrastructure Ports
PORT_CADDY_HTTP=${ports.caddy_http}
PORT_CADDY_HTTPS=${ports.caddy_https}
PORT_KEYCLOAK=${ports.keycloak}
PORT_KONG_PROXY=${ports.kong_proxy}
PORT_KONG_ADMIN=${ports.kong_admin}
PORT_VAULT=${ports.vault}

# Database Ports
PORT_POSTGRES=${ports.postgres}
PORT_MONGODB=${ports.mongodb}
PORT_REDIS=${ports.redis}
PORT_ELASTICSEARCH=${ports.elasticsearch}
PORT_MINIO_API=${ports.minio_api}
PORT_MINIO_CONSOLE=${ports.minio_console}

# MCP Service Ports
PORT_MCP_GATEWAY=${ports.mcp_gateway}
PORT_MCP_HR=${ports.mcp_hr}
PORT_MCP_FINANCE=${ports.mcp_finance}
PORT_MCP_SALES=${ports.mcp_sales}
PORT_MCP_SUPPORT=${ports.mcp_support}
PORT_MCP_JOURNEY=${ports.mcp_journey}
PORT_MCP_PAYROLL=${ports.mcp_payroll}
PORT_MCP_TAX=${ports.mcp_tax}
PORT_MCP_UI=${ports.mcp_ui}

# Web App Ports
PORT_WEB_PORTAL=${ports.web_portal}
PORT_WEB_HR=${ports.web_hr}
PORT_WEB_FINANCE=${ports.web_finance}
PORT_WEB_SALES=${ports.web_sales}
PORT_WEB_SUPPORT=${ports.web_support}
PORT_WEB_PAYROLL=${ports.web_payroll}
PORT_WEB_TAX=${ports.web_tax}
PORT_WEB_CUSTOMER_SUPPORT=${ports.web_customer_support}
PORT_WEBSITE=${ports.website}
